# Everything the kit needs, pip only.  No conda, no system packages.
#
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt
#
# The lower bounds are what the kit was built and tested against; the upper
# bounds are absent on purpose, so a newer release is not blocked.  If
# something breaks, pin to the tested versions in the comment after each line.

pypsa>=1.3.0          # tested on 1.3.0
highspy>=1.7          # the HiGHS solver; "pip install highs" is a different package
pandas>=2.2           # tested on 3.0.5
numpy>=1.26           # tested on 2.4.6
networkx>=3.2         # tested on 3.6.1
geopandas>=1.0        # tested on 1.1.4
matplotlib>=3.8       # tested on 3.11.1
